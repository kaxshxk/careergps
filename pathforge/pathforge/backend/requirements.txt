# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.0
pydantic>=2.9.0           # 2.9+ is first release with Python 3.13 wheels
python-multipart==0.0.9

# LLM / Agent
anthropic>=0.28.0
langgraph==0.2.0
langsmith>=0.1.75         # minimum required by langgraph 0.2.0

# Search
tavily-python>=0.5.0      # 0.3.0 hard-pins tiktoken==0.5.1 which has no Python 3.13 wheels

# RAG / Embeddings
tiktoken>=0.8.0           # 0.8.0+ has Python 3.13 wheels; older versions need Rust to compile
llama-index>=0.12.0       # 0.10.x requires numpy<2.0, incompatible with Python 3.13
chromadb>=0.6.0           # 0.5.x pins numpy<2.0, incompatible with Python 3.13
openai>=1.30.0

# Database
supabase==2.5.0

# Document parsing
pymupdf==1.25.3           # 1.24.10+ has Python 3.13 wheels

# Utilities
websockets==12.0
tenacity==8.3.0
